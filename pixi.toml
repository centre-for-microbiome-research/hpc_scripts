[workspace]
authors = ["Ben Woodcroft <benjwoodcroft@gmail.com>"]
channels = ["conda-forge", "bioconda"]
name = "hpc_scripts"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]
test = "pytest tests"

[dependencies]

[feature.dev.dependencies]
python = "*"
pytest = "*"

[environments]
dev = ["dev"]
